#include <stdio.h>

/**
 * Program that prints "programming is like building a mutlilinguage puzzle"
 * followed by a new line 
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
